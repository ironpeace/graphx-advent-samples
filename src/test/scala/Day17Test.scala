import org.scalatest.FunSpec
import org.apache.spark.graphx._
import com.teppeistudio.Day17

class Day17Test extends FunSpec {
	describe("Day17") {

		// describe("Test of isAlreadySendedAndShorterRoute") {
		// 	it("isAlreadySendedAndShorterRoute") {
		// 		val in = Array(
		// 			(1L, Array(Array(3L, 4L), Array(5L, 6L)), 3), 
		// 			(2L, Array(Array(7L, 8L)), 3)
		// 		)
		// 		assert(Day17.isAlreadySendedAndShorterRoute(1L, 1, in) == true)
		// 	}
		// }

		// describe("Test of addMyIdInRoute") {
		// 	it("addMyIdInRoute") {
		// 		val in = Array(
		// 			(2L, Array(Array(3L, 4L), Array(5L, 6L)), 3), 
		// 			(2L, Array(Array(7L, 8L)), 3)
		// 		)

		// 		val ans = Array(
		// 			(2L, Array(Array(1L, 3L, 4L), Array(1L, 5L, 6L)), 3), 
		// 			(2L, Array(Array(1L, 7L, 8L)), 3)
		// 		)

		// 		val out = Day17.addMyIdInRoute(1L, in)

		// 		assert(ans(0)._2(0).mkString(",") == out(0)._2(0).mkString(","))
		// 		assert(ans(0)._2(1).mkString(",") == out(0)._2(1).mkString(","))
		// 		assert(ans(1)._2(0).mkString(",") == out(1)._2(0).mkString(","))
		// 	}
		// }
	}
}